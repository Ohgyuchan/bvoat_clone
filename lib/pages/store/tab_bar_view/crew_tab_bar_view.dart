import 'package:flutter/material.dart';

class CrewTabBarView extends StatelessWidget {
  const CrewTabBarView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
